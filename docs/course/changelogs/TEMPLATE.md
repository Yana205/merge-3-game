# Lesson N — <Title>

- **Branch:** `feature/lesson-NN-<slug>`
- **Status:** 🚧 IN PROGRESS | ✅ DONE
- **Date:** YYYY-MM-DD

## Summary

2–4 sentences: what this lesson added to the merge-3 crystal game and how it fits
the existing systems (what it built ON, what — if anything — it replaced and why).

## Deliverable → code map

| Assignment deliverable | Where it lives | Notes |
|------------------------|----------------|-------|
| ... | `Assets/_Project/Scripts/...cs:NN` | ... |

## What changed

- `path/to/File.cs` — new / modified: one line on the change.

## Editor steps (things the headless run can't do)

Numbered, precise steps for the user to run in the Unity Editor (menu items to
click, components to add, assets to assign). Skip this section if the lesson is
pure code. Prefer pointing at an `[MenuItem]` automation script we shipped.

1. ...

## AI assistance (when the assignment asks for it)

- **Prompt used:** ...
- **Did the output work directly?** yes/no — what was fixed.

## Review findings

- `[fixed]` ...
- `[ok]` ...
- `[note]` ...

## Verification

How this was confirmed (compiles / logic traced / would-run steps). Note that
headless runs can't enter Play mode — list what the user should see when they do.
