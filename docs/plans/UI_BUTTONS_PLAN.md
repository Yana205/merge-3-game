# Plan: UI Button Assets

**Execution branch:** `feature/ui-buttons` · **Skill:** `/ui-buttons` · **Depends on:** nothing (blocks UI_MENU_PLAN)

## Goal

Replace default gray Unity buttons with styled, reusable button sprites
(normal + pressed states) that match the pixel-art gem aesthetic, applied to
existing buttons (`NextLevelButton`) and ready for the menu plan.

## Current state

- One real button in the scene: `Canvas/LevelCompletePanel/NextLevelButton`,
  default Unity sprite.
- `UIManager` also has a `gameOverPanel` field (panel may need building —
  covered by the menu plan, not this one).

## Asset options (pick one at execution time)

1. **Kenney UI Pack (recommended for speed + quality)** — CC0, purpose-built
   9-slice button PNGs in several colors/states, downloadable from
   kenney.nl/assets (UI Pack family). Zero license risk, professional
   quality. Slight style mismatch with pixel art — pick the squared/flat
   variants.
2. **Canva generation (best style match)** — generate a small UI kit sheet:
   pixel-art rounded-rect button frame in 2 states (raised / pressed) × 2-3
   colors, on magenta background; slice with the flood-fill pipeline (see
   `docs/ASSET_WORKFLOW.md`). Design for **9-slice**: uniform borders, plain
   center.
3. **Procedural** — 9-slice rounded rect generated by script with the game's
   palette. Cheapest, least character.

## Specs

- Button sprite ~192×64 source, 9-slice borders ≥16 px each side.
- States: normal, pressed (darker/offset), optional disabled (desaturated).
- Text: existing TMP font, white with dark outline for contrast.

## Integration steps

1. Assets → `Assets/_Project/Art/UI/` + CREDITS.md entry.
2. Import as Sprite; set `spriteBorder` for 9-slice via `assets-modify` or
   `script-execute` (TextureImporter.spriteBorder).
3. On each Button: Image type **Sliced**, assign normal sprite; Button
   `transition = SpriteSwap` with pressed sprite (via
   `gameobject-component-modify`).
4. Apply to `NextLevelButton` now; the menu plan reuses the same sprites.
5. Optional: a `Assets/_Project/Prefabs/UI/StyledButton.prefab` so future
   buttons are drag-and-drop consistent.

## Verification

Play mode; screenshot the level-complete panel (trigger by reaching target
score via a temporary `script-execute` call to the score/level-complete path,
or lower the target in a test level); confirm the button renders sliced
without corner stretching and the pressed state responds.

## Instructions for Claude

`git checkout -b feature/ui-buttons` from updated `main`. Follow this plan;
ask the user only which asset option (default 2 — Canva — since style match
was chosen for gems). Commit sprites + meta + prefab + component changes;
push, PR titled `feat: styled UI buttons`.
